# Pipelines: https://gitlab.webhare.com/webhare-opensource/runkit/-/pipelines

image: docker:latest

services:
 - docker:dind

stages:
- test

default:
  interruptible: true

runscripttests:
  stage: test
  script:
    - npm ci
    - RUNKIT_CI_SOURCEDOWNLOAD=1 ./tests/runtests.sh
